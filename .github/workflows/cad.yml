name: cad

on:
  push:
    branches: [main, "dcr/**"]
    paths:
      - "cad/**"
      - ".github/workflows/cad.yml"
      - "scripts/smoke/**"
    tags: ["cad-v*"]
  pull_request:
    paths:
      - "cad/**"
      - ".github/workflows/cad.yml"
      - "scripts/smoke/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: cad (${{ matrix.target }})
    # Public-repo standard runners are free across all OSes, so full matrix
    # runs on every trigger. (Matrix context isn't valid in job-level `if:`.)
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-x86_64
            runs-on: ubuntu-22.04
            goos: linux
            goarch: amd64
            ext: ""
          - target: linux-arm64
            runs-on: ubuntu-22.04-arm
            goos: linux
            goarch: arm64
            ext: ""
          - target: macos-universal
            runs-on: macos-14
            goos: darwin
            goarch: ""
            ext: ""
          - target: windows-x86_64
            runs-on: windows-2022
            goos: windows
            goarch: amd64
            ext: ".exe"
    runs-on: ${{ matrix.runs-on }}
    defaults:
      run:
        shell: bash
        working-directory: cad
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          # cad has no external deps (no go.sum); disable module cache to avoid
          # cache-dependency-path errors. Build cache still works automatically.
          cache: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build (native)
        if: matrix.target != 'macos-universal'
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p build
          go build -o build/cad-plugin${{ matrix.ext }} .

      - name: Build (macOS universal — lipo amd64 + arm64)
        if: matrix.target == 'macos-universal'
        env:
          CGO_ENABLED: "0"
        run: |
          mkdir -p build
          GOOS=darwin GOARCH=amd64 go build -o build/cad-plugin.amd64 .
          GOOS=darwin GOARCH=arm64 go build -o build/cad-plugin.arm64 .
          lipo -create \
            build/cad-plugin.amd64 \
            build/cad-plugin.arm64 \
            -output build/cad-plugin
          lipo -info build/cad-plugin

      - name: Go test (linux-x86_64 only — Linux is the source of truth)
        if: matrix.target == 'linux-x86_64'
        run: go test ./...

      - name: Smoke
        run: python3 ../scripts/smoke/mcp_smoke.py build/cad-plugin${{ matrix.ext }}

      - name: Pack release tarball
        run: |
          PLATFORM="${{ matrix.target }}"
          VERSION=$(python3 -c 'import json; print(json.load(open("manifest.json"))["version"])')
          mkdir -p dist
          PACKDIR="dist/cad-${VERSION}-${PLATFORM}"
          mkdir -p "$PACKDIR"
          cp build/cad-plugin${{ matrix.ext }} "$PACKDIR/"
          cp manifest.json "$PACKDIR/"
          # SHA256SUMS covers the binary AND manifest.json — Minerva's
          # installer will need both verified for a trustworthy install.
          if command -v sha256sum >/dev/null 2>&1; then
            (cd "$PACKDIR" && sha256sum cad-plugin${{ matrix.ext }} manifest.json > SHA256SUMS)
          else
            (cd "$PACKDIR" && shasum -a 256 cad-plugin${{ matrix.ext }} manifest.json > SHA256SUMS)
          fi
          tar -czf "dist/cad-${VERSION}-${PLATFORM}.tar.gz" -C "$PACKDIR" .
          ls -la "dist/cad-${VERSION}-${PLATFORM}.tar.gz"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cad-${{ matrix.target }}
          path: cad/dist/cad-*-${{ matrix.target }}.tar.gz
          if-no-files-found: error
          retention-days: 14

  release:
    name: Publish ${{ github.ref_name }} GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/cad-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all matrix tarballs
        uses: actions/download-artifact@v4
        with:
          pattern: cad-*
          path: artifacts/
          merge-multiple: true

      - name: List downloaded artifacts
        run: ls -la artifacts/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/cad-*.tar.gz
          generate_release_notes: true
          fail_on_unmatched_files: true
