# Manifest of the gd test suites pcb/scripts/run-gd-tests.sh MUST find under
# pcb/tests/gd/. Line format (v2, bug 019ff2b1fccb + 019fa83e8310):
#
#     <filename> [assertions=N] [real-worker]
#
# - <filename>: exactly as it appears on disk. A suite going missing
#   (deleted, renamed, moved off the glob) or appearing unregistered is a
#   loud, named pre-flight failure in both directions.
# - assertions=N: the suite must report exactly N total assertions
#   (passed+failed). This is what makes `check(true); quit(0)` planted above
#   the real assertions FAIL instead of passing the old `n_pass > 0` floor.
#   When a suite legitimately grows or shrinks, re-pin here in a deliberate,
#   reviewed own-commit — the same act as registering the suite.
# - real-worker: the suite's Results line must prove real_worker_used=true.
#   Its canned subprocess-boundary fallback keeps it runnable on a machine
#   without the pcb-plugin binary, but can never satisfy the gate.
#
# This file is deliberately NOT named test_*.gd (it must not match the
# runner's own `test_*.gd` glob) and NOT executable GDScript.
# See pcb/docs/gd-tests.md.
# The pad-to-pad half of the bus geometry (bundle_routes + its breakout
# legs), including the refusals: a crossing the leaves-first rules cannot
# order, a spine too short for its fan-outs, and a pad past the spine's end.
#
# Re-pinned 53 -> 61 by the cross-provider review's two P1 findings, both of
# which REPRODUCED against the module at 0cfc3f5 (routed, ok == true, copper
# emitted): a breakout leg crossing a lane belonging to a distant arm of a
# folded-back spine, and two pad legs running parallel 0.25mm apart where the
# rule demands 0.5mm. The whole-route clearance measurement that refuses both
# adds six assertions in its own section, and the invariants walk's new metric
# oracle adds one per routed case. 61 is MEASURED (61 passed, 0 failed), never
# derived by reading.
# 61 -> 95: refusals split into unbuildable vs bad-but-buildable (nine cases
# reclassified onto a helper that also demands the geometry), the crowded-
# copper finding's measured/required/witness checks, and the via station
# (hand-derived fan, split indices, via pitch, two refusals, one finding, and
# two more invariant cases). MEASURED: 95 passed, 0 failed.
# 95 -> 100: the leg-station snap (a leg lands on its lane's mitered corner,
# never leaving a sub-width stub) adds the hand-derived BCLK case and a
# min-segment-length walk over every route; the batch review dropped one
# check that could not fail on its own. MEASURED: 100 passed, 0 failed.
# 100 -> 116: pads lying along the spine fan out sideways (hand-derived U1S
# column, foreign-pad reach, no shared copper, Manhattan and no stub: +12),
# the corridor words follow the rule they measure (+3), and a staggered pair
# is pinned to stay on the ladder (+1). MEASURED: 116 passed, 0 failed.
# 116 -> 122: a pad already under its lane's copper gets no reach at all, and a
# pad half-to-one track width off its lane is named bus_sub_width_stub (+6).
# MEASURED: 122 passed, 0 failed.
test_bus_breakout_geometry.gd assertions=123
# The canvas bus tool's pin-to-pin gesture and the plan its two MCP verbs
# share. The board triple (traces, history, change journal) is the oracle for
# every "this gesture wrote nothing" claim, read before the commit verb.
# 96 -> 121: pads are picked by distance to their copper (section 4b), where
# each net may end is shown from the first pick (section 9), and the bus picker
# and the shared hit test are held to one answer on a tie. MEASURED on a run,
# not counted: section 9's checks sit inside a two-iteration loop, which is how
# a source count of this suite goes wrong.
# 121 -> 148: the double-click PATH ending (section 5 grew a second gesture, a
# refused-first-press probe and a too-short refusal), the per-net airline test,
# and the teach-line check. MEASURED on a run; the source count is lower
# because section 5's route checks expand per net.
# 148 -> 218: the finish double-click on the last landed target, the
# crossing bus that lands and is named, findings reaching the verb reply and
# the ghost, the layer switch that becomes a via station (gesture and verb,
# compared board to board), a fractional verb index and a torn station plan
# both refused. MEASURED: 223 passed, 0 failed.
# 223 -> 247: the foreign-copper findings (every pad, via or trace the bus
# lands on or crowds, reaching the verb reply, the status line and the
# ghost), the working-layer funnel into the via station, and the teach /
# status wording checks. Implementers reported 225 by reading; the run says
# 247, because the findings walk expands per foreign entity. MEASURED.
# 247 -> 251: section 13c, a station via that lands on one-sided copper is
# reported on that copper's layer, two sides x two checks. MEASURED: 251.
# 251 -> 258: the SOURCES note lost its speculative branch — 13b re-pins the
# teach line and the verb reply to carry no start-on advice (+1 net), and 13d
# pins that targets with no copper on the working layer STILL name the layer
# to set, and go quiet once the working layer is theirs (+6). MEASURED: 258.
# 258 -> 289: a pad with no copper on the landing layer is named (14/14b,
# +12), a station that walls a pad off is named (13e/13f, +9), and the
# add_bus journal row (13g, +10). MEASURED: 289 passed, 0 failed.
# 289 -> 291: an unplated hole is on no layer at all (+2). MEASURED: 291.
# 291 -> 362: open-ended commit (15/15b, +27), every lane wears its net (16,
# +15), lane order by number click + clean-order advisory (17/17b, +29
# incl. the path-start-beside-a-pick pin). MEASURED: 362 passed, 0 failed.
# 362 -> 375: a pad under the number glyph is picked (17c), the TARGETS plan
# is the commit plan (17d), an all-open station bus (17e). MEASURED: 375.
test_bus_tool.gd assertions=419
test_candidate_canvas.gd assertions=281
# Bug 01a007f1dd02 — canonical wire form for the worker channels. Pinned at
# authoring (2026-08-19); the checked-in fixture is the REAL smart-remote-v2
# enriched dict, so the suite re-demonstrates the >64KiB defect condition on
# every run before proving the projection fixes it.
test_canonical_wire_board.gd assertions=26
test_connectivity_baseline_surface.gd assertions=21
# SR2FAB S6 — authored, never executed; pinned at testex like the entry
# below it.
test_draft_check_seam.gd
test_epoch6_fix_regressions.gd assertions=9
# SR2FAB S1 — the export-YAML verb and button. Pinned from the first full run
# that executed it (34 checks, all outside loops): the button's four status
# outcomes, the promote refusals, and the verb's own reply shape.
test_export_yaml_verb.gd assertions=43
# Task-cycle 12 B4 — the exporter chooser, the View menu's export rows and
# minerva_pcb_board_export driven over ONE canned worker reply and compared to
# each other. Authored under this cycle's static-gates-only rule and NEVER
# EXECUTED, so it carries no assertion pin: measure it at the scoped run and
# pin it there, the same way test_draft_check_seam.gd and test_mm_quantization
# were handled.
test_export_surfaces_parity.gd
test_geometric_drc_surface.gd assertions=31
# GA-1 grew this suite (stack_shape_error matrix, stack_for_count,
# inner_index_any, the set_board_layers model/handler section) and retired the
# dead default_two_layer checks; the exact count re-pins at GA testex from the
# first trustworthy run, per the OFC precedent below.
test_layer_stack.gd assertions=105
# SR2FAB S10 — authored, never executed; pins at testex.
test_mm_quantization.gd
test_manifest_tool_registration.gd assertions=45
# SR2FAB S8 — authored, never executed; pins at testex.
test_mounting_holes_verb.gd
test_pad_synthesis.gd assertions=8
test_parity_bridge.gd assertions=79
test_pcb_annotation_host_semantics.gd assertions=181
test_pcb_apply_route_hints.gd assertions=89
test_pcb_backend_lifecycle.gd assertions=52
test_pcb_bus_geometry.gd assertions=90
# 123 -> 121: the collapsed-Properties width-reveal case left with the
# Properties section (work item 01a04b9c9064); its probe floor is 6 -> 4.
test_pcb_canvas_input_probe.gd assertions=121
test_pcb_cutout.gd assertions=38
test_pcb_drc_propose.gd assertions=23 real-worker
# Pinned at OFC testex 2026-08-13 — the first run where all three proved
# real_worker_used=true (fixture repairs: resolvable footprints, authored
# design_rules, in-bounds parts, un-connected BR span; see hint
# 019ff9a0b471).
test_pcb_explicit_propose.gd assertions=80 real-worker
# The canvas hover card: content vs the fixture, content vs the read verbs
# (describe_component / describe_region), the placement rules, and a live
# pointer. AUTHORED count: 8 + 8 + 10 + 29 + 12. The placement section's 29
# is 7 anchors x 3 checks inside one loop, plus 8 outside it, so the source
# reads 18 fewer check( calls than it runs.
# 64 -> 67: the OVERSIZED card — a card bigger than the canvas is clipped to it
# rather than painted past the panel edge (both axes, then one axis with the
# pointer rule still to satisfy).
# Counted by hand from the check() call sites: 3 new, none inside a loop.
# 67 -> 81: section 6, the Options toggle (work item 01a04b85e620) — verb
# read/apply of the key, no card on hover while off, the pin-inspector click
# that raises one anyway, its release when the toggle returns, and the 19 px
# clearance. 14 new check() calls, none inside a loop.
# 81 -> 110 (work item 01a04b9c9064): section 6's "hands back to hover" check
# becomes two (a pinned card survives empty board, a different pad replaces
# it: +1), and section 7 adds zone and via cards vs the fixture (6 + 7), vs
# describe_zone / list_vias (4 + 6), the Group line (3) and a live pointer over
# each (2) = 28. None inside a loop; counted from the diff's check( lines.
test_pcb_hover_card.gd assertions=110
test_pcb_hint_refine_loop.gd assertions=64 real-worker
test_pcb_single_trace_tool.gd assertions=98 real-worker
test_pcb_legacy_annotation_migration.gd assertions=53
# The pin→net invariant (a pin belongs to at most one net): connect MOVES,
# minerva_pcb_disconnect_net removes, each is ONE undo step, and get_nets /
# pin_info / the board dict handed to pcb.serialize are held to one answer.
# 51 -> 70: a membership write that cannot be done is refused rather than
# half-done (section 6) — an unknown pin and a malformed entry refuse the whole
# connect, a net-guarded disconnect removes only that net's membership, and
# move/swap refuse a pin held by two nets by name. MEASURED: 70 passed, 0 failed.
test_pcb_net_membership.gd assertions=70
# SR2FAB S9 added ONE assertion (get_layout_state carries
# fabrication_stage). 149 -> 150 is an AUTHORED count; see the note on
# test_draft_composer.gd above.
# 154 -> 163: the read-only component rows moved to the canvas hover card, so
# the three read-out assertions are replaced by two "no read-out row survives"
# checks, a nine-name loop over the editing controls that MUST survive, and one
# that a selection still drives the section. AUTHORED: -3 +12.
# 163 -> 148 (work item 01a04b9c9064): the Properties section is gone, so §7's
# 15 checks become 6 (four "no such node", the fabrication row, its picker), and
# the 6-check trace-width reveal test (§10) went with the reveal. -15 +6 -6.
# 148 -> 154 (sidebar width + dividers, same work item): wide +2 (owned
# width for 1100 px, above the two-button floor), medium +2 (owned width for
# 600 px, two tools on one row), §7 +2 (no divider above the fabrication row,
# dock divider hidden off-wide).
# 154 -> 156: the dock-migrates test checks the sidebar content fills the
# scroll viewport and the dock parent gets the column's slack (vertical
# expand on RightSidebarContent).
test_pcb_panel_layout.gd assertions=156
test_pcb_panel_model.gd assertions=220
# 211 -> 217: the board-resize undo step (the verb snapshots, and one undo
# restores the previous outline) — six checks, HAND-COUNTED from source.
test_pcb_panel_tools.gd assertions=217
test_pcb_panel_tools_migration.gd assertions=75
# The pad codec's fab optionals, the roundrect land at its authored corner
# radius, and the load-time indeterminate lead.
test_pcb_pad_fidelity.gd assertions=47
# Epoch NLC (154 -> 158): the Tools-area Via button adds four assertions to this
# suite's tool-button walk. Found by RUNNING it after the DCR picker removal —
# it is outside the NLC delta the owner scoped, so nothing had exercised it since
# the button landed. All 158 pass; only the pin was stale.
# 158 -> 183: the Bus button's phase badge adds one wide test of 25 assertions
# (_test_bus_phase_badge). Every check() in it is unconditional and none sits
# inside a loop; measured on a run, which agreed with the source count.
# 183 -> 200: a refused bus plan is named in the standing status line and worn
# by the badge — one wide test of 17 assertions (_test_bus_refusal_is_held).
# Its one loop ANDs three states into a single check(), so the source count of
# check( calls is the assertion count.
# 200 -> 202: the standing bus status line carries the lane map and the
# tooltip its untrimmed form (+2). MEASURED: 202 passed, 0 failed.
# 249 -> 230 (work item 01a04b9c9064): the context menu lost "Set trace
# width…" with the Properties width row it revealed — 13 checks out of the
# trace-menu test (the item, the select-and-reveal, the row commit, the
# three-tier expand loop) and the 6-check BT-68 focus test. Counted from source.
test_pcb_panel_ui.gd assertions=241
# 49 -> 57: the copper-extent pad pick adds four probes run over a part built
# twice, unrotated and at 90 degrees. MEASURED on a run — the four checks sit
# inside that two-part loop, so the source reads eight fewer than it runs.
# 57 -> 79: the Pin Info sidebar section is gone, so its four readout
# assertions become hover-card ones (net -1 / +1 / +1), and two new sections
# land — D2 compares each of three pads' cards against the minerva_pcb_pin_info
# reply for the SAME pad (5 assertions x 3 probes = 15, the roles if/else being
# two source lines but one assertion), and D3 pins the card's paint invariants
# (6). AUTHORED: -1 +1 +15 +6 +1.
# 79 -> 80: an inspector click PINS the clicked pad's card, read after the
# pointer moved onto empty board (work item 01a04b9c9064).
test_pcb_pin_inspector.gd assertions=80
test_pcb_ratsnest.gd assertions=157
# OFC-6 (019ff942f1f8) flipped the two stale pre-cutover .minpcb checks to
# the current core contract — verified 43/0 at OFC testex on the dev
# scaffold.
test_pcb_plugin_smoke.gd assertions=43
test_pcb_route_hint_mcp_parity.gd assertions=19
# Section 7 (the double-click's second press) added 10 unconditional check()
# calls: 54 -> 64.
# Section 8 (the netless refusal's anchor vocabulary) adds 13: 2 rule checks,
# 1 landed-on-the-pad guard, 3 static sites in a 2-kind loop, 4 on the MCP
# verb — 64 -> 77.
# Section 9 (the working layer authors, the view only shows): 9 checks that
# choosing a layer moves where the trace lands and hiding or filtering a
# layer does not — 77 -> 86. MEASURED: 86 passed, 0 failed.
# 86 -> 110: section 10, a free trace end is an anchor — start extends the
# same trace, finish joins by extending, netless/joined/pad-wins refusals,
# and minerva_pcb_add_trace agrees (+24). MEASURED: 110 passed, 0 failed.
# 110 -> 116: nearest free end across a short stub, a locked trace offers
# none, and finishing on the same trace's other end is refused (+6).
# 116 -> 120: the model itself refuses a locked trace and an end that gained
# copper between the pick and the write (+4). MEASURED: 120.
test_pcb_trace_tool.gd assertions=153
# Re-pinned 28 → 34 with OFC-5's menu-revealed drawing-width section (BT-OFC5,
# 6 checks; the section-registry count check updates in place).
test_pcb_width_prefs.gd assertions=34
test_pcb_workflow_kinds.gd assertions=131
test_production_seam.gd assertions=96
test_proposal_attribution.gd assertions=33
test_routing_workspace_model.gd assertions=168
test_routing_workspace_v2.gd assertions=395
test_trace_identity_delete.gd assertions=213
# Stable copper identity and honest ownership. Group 2 drives the REAL
# pcb-plugin binary (pcb.serialize -> pcb.deserialize, the exact codec
# export_yaml/load_board use), so the id round trip is measured rather than
# modelled -> `real-worker`.
test_copper_identity_ownership.gd assertions=41 real-worker
test_workspace_check.gd assertions=46
# 77 -> 84: the reply's effective width outranks a hint width when the segments
# carry none, and an unresolved width reaches the draft checker as 0.0 rather
# than a nominal 0.25. MEASURED: 84 passed, 0 failed.
# 84 -> 91: ...except when that stamp names a DEFAULT source (board_rules /
# engine_default), which loses to an authored hint and is picked up again only
# as the last rung. MEASURED: 91 passed, 0 failed.
test_workspace_ingest.gd assertions=91
# 76 -> 85: group 10, a persisted via with no dimensions restores UNRESOLVED and
# takes the board's design_rules at commit (with an authored size as the
# negative control). MEASURED: 85 passed, 0 failed.
# 85 -> 88: a NEGATIVE stored dimension is malformed, so the via record is
# dropped and named rather than clamped into the "unstated" 0.0 that the board
# then answers. MEASURED: 88 passed, 0 failed.
test_workspace_persistence.gd assertions=88
# 1189 -> 1192: the width-provenance group grew the copper-creating half — the
# segments' own width_mm outranks the hint and the board, and a route drawn at
# two widths commits both. MEASURED: 1192 passed, 0 failed.
test_workspace_tools.gd assertions=1192
test_zone_select_delete.gd assertions=50
test_ux3_contracts.gd assertions=44
test_staged_entities.gd assertions=72
# Re-pinned 34 → 49 with OFC-2's placement-composition section (15 checks),
# then 49 → 68 with OFC-3's provenance + commit-gate section (19 checks),
# then 68 → 82 with OFC-4's board_check census section (14 checks),
# then 82 → 91 with the Codex-1188 repair round (F1 race seams ×4 in 1c,
# F3/F4/F5 census checks ×5 in 1d).
# SR2FAB S9 grew the board_check census section by FOUR assertions
# (complete_by_declaration: absent on a genuinely complete board, present
# on a declared one, absent on an incomplete one). 91 -> 95 is an
# AUTHORED count — nothing was executed this epoch — so if the first
# trustworthy run disagrees, the runner fails loudly with the real
# number and this line is corrected. That loud failure is the pin
# working, not the pin being wrong to keep.
test_draft_composer.gd assertions=95
test_staged_canvas.gd assertions=88
# Pinned at OFC testex 2026-08-13 (first post-await-repair full run).
test_staged_verbs.gd assertions=73
test_draft_doorways.gd assertions=31
test_staged_placement.gd assertions=114
# NEW at epoch GA-6 (bug 019fa0f8d575): the typed-Dictionary guard matrix for
# both panel files' _dict_or_empty. Assertion count pins at the GA testex
# from the first trustworthy run.
# SR2FAB S7 — authored, never executed; pins at testex.
test_degenerate_copper.gd
test_dict_guard.gd assertions=30
# NEW at epoch NLC C1 (item 019fff6080fd): inner-layer vias — C1a's refuse-don't-
# guess guard (groups 1-4) and C1b's placeable continuation layer (groups 5-6).
# Grown to 39 by the cold review's finding 1: group 7 pins the case C1a's first
# guard did NOT close — a mixed [F.Cu, In1.Cu] authored payload, whose running
# layer IS toggleable, so the walk proceeded and clobbered the authored inner
# run. Count is 39 by construction (7 + 6 + 3 + 3 + 8 + 6 + 6). NOT YET
# CONFIRMED BY A RUN — the epoch defers all test execution until after the Codex
# review, so this pin is provisional and re-pins at testex from the first
# trustworthy run, exactly as test_layer_stack.gd and test_dict_guard.gd did.
test_inner_layer_via_guard.gd assertions=39
# NEW at epoch NLC C4 (item 019ffeaccc0c): minerva_pcb_view_state. Count is 37
# by construction (10 + 7 + 9 + 11), grown to 46 by the cold review's finding 5:
# group 5 pins TYPE validation (a non-boolean flag value, a non-object `flags`,
# an empty layer name), each of which could previously slip past the name-only
# validation and land a partial write. Grown again to 57 by the CODEX review's
# finding 1: group 3 now asserts ACTUAL visibility (is_layer_visible) with the
# toolbar deliberately left on a specific layer — the state in which soloing
# used to change the eyes and nothing on screen — and group 6 pins the newly
# writable trace_layer_filter. Provisional for the same reason as the
# suite above — no run happens before the Codex review — and re-pins at testex.
# 57 -> 76: group 7 pins the split between the working layer (what edits
# land on) and trace_layer_filter (what is drawn): setting one leaves the
# other alone, and re-asserting the same value changes nothing. MEASURED:
# 76 passed, 0 failed — the first run of this suite; the 57 above was never
# executed and the run confirms the batch's count, not the provisional one.
# 76 -> 95: group 8 pins fit:true against the CURRENT viewport rect — a narrow
# docked pane must not leave the board a sliver. Three viewport aspects x five
# checks, plus the bare-board case and the overhanging part. DERIVED, not
# measured (2 + 5*3 + 2 = 19). Confirm on the first run.
test_view_state.gd assertions=95
# NEW at epoch NLC C2 (item 019fff60e05a): minerva_pcb_place_via, the direct
# board via. Grown at C3 (item 01a001c39aa3) with minerva_pcb_add_trace's two
# groups — SAME suite on purpose: the pair is one parity gap answered twice and
# has to stay symmetric. Grown again in the canvas round: group 7 pins the
# HUMAN's Via tool (a registered toolbar button + a click that lands copper
# through the same model rule the MCP verb reads) and group 8 pins that rule
# directly.
#
# COUNT = 71 (70 + one assertion added at testex: the canvas Via tool snaps to
# the authoring grid, so the placed point is checked against _author_point AND
# bounded near the click, rather than against the raw click point).
# PREVIOUS COUNT = 70. That is 66 STATIC call sites plus 4 for the extra iterations of
# group 2's three-key loop — the counting rule's "+ extra loop iterations" term.
# Per group the static counts are 10, 3, 12, 6, 11, 11, 9, 4; group 2's 3 static
# sites execute 7 times, which is where the +4 comes from. Stated as static-vs-
# executed on purpose: an earlier version of this comment mixed the two and
# printed an arithmetic that did not add up, which would teach the next
# re-pinner the wrong rule (cold review, finding 8). Provisional until testex.
test_direct_copper_verbs.gd assertions=147
# DCR 01a0033a12a9 changes 2 + 3 — minerva_pcb_update_via (editing a PLACED
# via: place/delete/list existed and nothing could ADJUST one, so an agent
# could delete and re-create a via, losing the id, but never move or re-net it)
# and minerva_pcb_fabrication_stage (the board's declared manufacturing intent,
# so a via-only board's unrouted nets read as the job rather than as defects).
#
# COUNT = 93, VERIFIED BY EXECUTION (93 passed, 0 failed) — not hand-counted.
# That is 89 STATIC call sites + 4 for the extra iterations of group 3's
# three-key span loop (2 static sites executing 3 times). Stated static-vs-
# executed on purpose, following the sibling entry above — mixing the two is
# how this pin has been got wrong before.
#
# 92 -> 93 before the first commit: group 7 gained a fixture assertion after
# MUTATION showed its net-picker check was VACUOUS. It picked dropdown index 1,
# which sorts to "GND" — the net the fixture had already placed the via on — so
# it passed with the entire GUI commit path deleted. The added assertion pins
# that the chosen net differs from the one the via carries.
# 93 -> 81: group 7 (the sidebar via rows) is gone with the Properties section
# — via net/size/drill are edited by minerva_pcb_update_via only (work item
# 01a04b9c9064). The fabrication picker's group 11 stays.
test_via_edit_and_fab_stage.gd assertions=81
# NEW (work item 01a0223ec9e271269fd664fcf90dd20b): board-by-path + digest —
# the panel-sender half of the 64KiB broker-cap regression (route/serialize/
# deserialize all inlined O(board) payloads). COUNT = 63 by construction
# (Section 0: 22, Section 0i consumption: 6, Section 1: 33, Section 2: 2)
# after two cold reviews grew it — tests review: draft_request route variant,
# OS-native path pin, non-mutation + retry/prune lifetime, yaml_path reply
# consumption; fix review: the promote GATE sender (1f — it rides the same
# capped pipe one hop before promote's serialize), the export consumer at the
# LIVE envelope depth (1g), and consume-on-read for reply documents (0i6).
# Provisional until the first trustworthy post-fix run, per the sibling NEW
# entries above; the deliberate RED baseline run (helper + wiring absent)
# reports fewer and fails — that failing run is the point, not a stale pin.
test_board_by_path.gd assertions=137
# NEW (DCR 01a022ab356c acceptance): "proposal" means ghost trace only —
# B one-call span-propose (mints the intent internally; spans+hint_ids =
# union), C orphan-reroute terminal fallback with ingest task-key override,
# census pins, and re-entrancy (the fallback generation itself reroutes), D
# vocabulary by node name + text sweep. COUNT = 44 by construction (baseline
# 4, B 18, C 18, D 4) after the cold review's rework (two forever-red reads
# corrected: new generation from candidates[0], shim echoes worker-style
# attribution). Provisional until the first trustworthy post-DCR run; the
# deliberate RED baseline reports FEWER (conditional skips) and failing —
# that red run is the acceptance baseline, not a stale pin.
test_dcr_proposal_ghost.gd assertions=44
# NEW (bug 01a022b1b7d5 acceptance): unanswered intent's task-owned corridor
# constraint must survive save/reload (VALUES, not counts — cold review f1)
# and quarantine — the sidecar zero-payload rule counts constrained tasks as
# payload; bare tasks stay non-payload. COUNT = 14 by construction (group 1:
# 11 incl. quarantine, group 2: 3 incl. the write_envelope seed). Provisional;
# the deliberate RED baseline (rule unfixed) reports FEWER (conditional skips)
# and failing — that red run is the acceptance baseline, not a stale pin.
test_sidecar_constraint_survival.gd assertions=14
# NEW: the compiled pour fill reaching the model that answers what is already
# joined — the transport, not the fill rule (test_pcb_ratsnest.gd section 7
# owns that). Real-worker: sections 1 and 2 compare the COMPILER's regions
# against what the model holds, and a canned fill can only agree with itself.
test_zone_fill_transport.gd assertions=31 real-worker
# NEW: the one trace-geometry library (point/segment/polyline primitives,
# gaps, axis checks, trace-end test, translate, bounds), every case
# hand-derived. MEASURED: 89 passed, 0 failed.
test_pcb_trace_geometry.gd assertions=89
# NEW: cut a trace at an interior vertex — model op, refusals, the verb's
# index and coordinate forms, the free end afterward, the context menu.
# MEASURED: 43 passed, 0 failed.
# 43 -> 49: a locked trace refuses the verb; a cut or extend retires the
# committed candidate that owned the trace, canvas and verb (+6).
# 49 -> 50: cut_trace refuses a locked trace on its own (+1). MEASURED: 50.
test_trace_cut.gd assertions=50
# NEW: the panel half of the SHARED pad-contact vectors under spec/contact —
# the worker runs the same directory (worker/tests/test_copper_contact_vectors.py),
# so a case the two sides answer differently cannot be added without one of
# them going red. UNPINNED on purpose: the assertion count is one per vector
# directory plus the fixed sections, so pinning it would make adding a vector a
# two-file edit for no gain.
# The fixed sections are the two things a vector cannot state: the unknown-land
# disc (no vector is sizeless) and the LOADED-BOARD seam — inline-authored
# lands, which every vector bypasses by assigning `pads` directly. The latter
# is the bench's R9 row and is +10 assertions; the total stays unpinned rather
# than carrying a DERIVED count, since a count here must be MEASURED on a run.
test_copper_contact_vectors.gd assertions=54
# A same-net pour is a terminator for the canvas Trace tool, and an unplated
# hole joins nothing — including for the ratsnest, which must never let a hole
# with no barrel answer whether a join crosses layers.
test_trace_pour_terminator.gd assertions=12

# Via ownership (propose_via for_hint), design-rule via sizing at proposal time,
# no invented trace width, and the panel half of layer-hop waypoints.
# 59 -> 68: inserting or deleting a bend BEFORE a layer-carrying one leaves the
# hop on its own corner (5f/5g/5h). MEASURED: 68 passed, 0 failed.
test_hint_hops_and_via_owners.gd assertions=68

# Pin Select, the one land-to-world transform shared by the renderer and the
# hit test, and the rotation convention a placement reply states.
# 106 -> 110: an UNPLATED hole is on no copper layer ("none"), not on all of
# them, and the row agrees with the contact predicate. MEASURED: 110 passed.
# 110 -> 113: a logical pin with several physical lands reports from the
# copper-bearing ones, so an NPTH record listed first no longer answers "none"
# for a pad that plainly renders and routes. MEASURED: 113 passed, 0 failed.
test_pcb_pin_select.gd assertions=113

# minerva_pcb_describe_region (ONE read of a board rectangle) and
# layers_touched on it and on minerva_pcb_list_vias.
test_describe_region.gd assertions=146

# A transient status message must not be wiped early by the PREVIOUS message's
# uncancelled 2s timer. Section 1 measures that in REAL TIME and so takes ~3.5s
# of wall clock; that cost is the point — the rule is about a clock.
test_transient_status.gd assertions=17

# Board-level silk text and graphics, panel half: the GDScript font mirror's
# fixture numbers, the B.SilkS mirror convention asserted point-for-point with
# negative controls, the board_graphics round trip through
# to_board_dict/from_board_dict, one-undo-step add and delete, the three MCP
# verbs, and canvas pick/select/delete.
# 61 -> 75: bounds report the INK (half a stroke past the centreline), an
# unminted supplied id is refused before the Go gate sees it, several polylines
# plus one id is refused, and both selection seams reach a board graphic.
# MEASURED: 75 passed, 0 failed.
test_board_graphics.gd assertions=75

# Adding a part BY LIBRARY FOOTPRINT REF, against the real worker: the added
# part carries the pads the worker itself reports for that footprint, the geometric DRC over the board stays determinate and clean,
# one sketch part turns that verdict indeterminate and is named at add time by
# the reply AND by the panel's census AND by the worker's refusal, and an
# unresolvable ref is refused with the board untouched.
# NOT YET PINNED: assertions= must be set from a MEASURED run — see the header.
test_add_by_library_ref.gd assertions=46 real-worker

# The four geometry contracts that need no worker: the rotation WORDS mean
# what the screen shows (asserted on where a pad landed, never on the rotation
# number), a two-column part reads its columns as west/east whatever its aspect,
# the `pads` key is the board's geometry authority rather than a payload
# optimisation, and the drawn designator is a render of the LIVE ref placed
# clear of the part's own courtyard.
# 38 -> 42: the designator-clearance block — the courtyard fixture's own top
# edge, the drawn ref above it, the stroke-width margin, and the teeth (the
# pre-derivation anchor draws inside the switch body). Counted by hand from the
# check() call sites.
# 42 -> 57: section 5, minerva_pcb_set_refdes — the read (anchor, empty
# changed, a board-frame box straddling the label), the move (box translates by
# the delta, does not resize, changed names both fields), one undo (one history
# step, the anchor back, the label back), the panel-state round trip (anchor and
# strokes), the two refusals plus the untouched anchor, and hidden. Counted by
# hand from the check() call sites: 15 new, none inside a loop.
# 57 -> 66: section 6, the authored placement as BOARD STATE — an untouched
# component states neither key, a set one states the authored placement and
# still no derived anchor, a fresh model loaded from that document redraws the
# same designator and re-states the placement, a one-field block moves that
# field and inherits the rest, and the placement survives the wire trim.
# Counted by hand from the check() call sites: 9 new, none inside a loop.
# 66 -> 71: the verb accepts a literal advisory-shaped suggestion whole, a
# partial write authors only the fields the caller sent (hidden on, hidden off),
# and a restated value is authored anyway while `changed` stays empty.
# Counted by hand from the check() call sites: 5 new, none inside a loop.
# 71 -> 72: the reported designator box is the printed INK — the centreline
# span grown by one whole stroke width — measured against a raw stroke span the
# box function shares no arithmetic with.
# Counted by hand from the check() call sites: 1 new, not inside a loop.
# NOT YET PINNED: see above.
test_part_geometry_contracts.gd assertions=66

# The fab preview a human can read: every palette ink is MEASURED against the
# canvas ground (isolated to WCAG 4.5:1, composited to 1.4.11's 3:1) and the
# end-to-end arm samples the rasterized pixel of a verbatim emitted silk layer —
# headless has no rendering device, so this is Image rasterization, not a
# screenshot. Plus the banner that never touches the board rect, the emitted-
# layer picker through both the View menu and minerva_pcb_view_state, and both
# stale paths retracting show_fab_preview with a visible reason.
# 78 -> 100: section 7 (only an edit that reaches the emitter takes the preview
# down — a re-load of the same board, an MCP preference write and a view change
# leave it standing, a component move and a board resize do not), section 8 (a
# fitted capture takes the same camera as the screen, with the board fit as the
# differing control) and section 9 (black holes, a dark purple outline, the rim
# they are rasterized in, and the picker swatches that name them), plus one more
# contrast arm proving every rimmed ink would fail without its rim.
# Counted by hand from the check() call sites: 22 new, none inside a loop.
test_fab_preview_legibility.gd assertions=100

# Four claims about what the board canvas paints, all observed through
# recording_canvas.gd (the painter-call spy): a bottom part's authored artwork
# prints on the FLIPPED layer; a closed layer eye takes that layer's LANDS with
# it and not only its traces; a placement ghost draws its lands at the ghost
# pose, which is what a reviewer judges clearances by; and a part with real land
# geometry draws no nominal pin markers over it.
# Counted by hand from the check()/check_eq() call sites: 16 + 6 + 10 + 6, none
# inside a loop.
test_canvas_render_truth.gd assertions=38
test_refdes_anchor_document_load.gd assertions=12 real-worker

# A PCB tab's note carries the BOARD, not a picture of one: the plugin_data
# payload restored into a fresh model matches the source's whole-board token
# (taken over the saved form — the machine's library resolution is deliberately
# left behind, and read on both sides), the preview is the capture the module
# ASKS FOR (headless has no rendering device, so a stand-in canvas records the
# size and the fit flag and returns an Image of exactly that size), the caption
# is one exact hand-computed string, and a payload from another schema version
# is refused without touching the loaded board.
# Counted by hand from the check() call sites: 4 + 4 + 6 + 3 + 5, none inside
# a loop.
test_pcb_note_hooks.gd assertions=22

# The per-component `assembly` block survives a promote. The panel does not
# model it — it rides PcbComponent.canonical_extra, the verbatim passthrough —
# so the suite drives the passthrough (board dict, duplicate, undo codec), then
# the REAL worker over pcb.serialize -> pcb.deserialize, checking every field
# and every authored placement ref on the board that comes back, plus the
# legacy `assembly: exclude` scalar arriving migrated and a board with no
# assembly data arriving with no assembly key.
# Counted by hand from the check() call sites: 6 + 14 + 3, none inside a loop.
test_assembly_schema_roundtrip.gd assertions=23 real-worker

# The component-reading verbs (get_components / describe_component) carry the
# board's authored `assembly` block and the COMPILER's resolved physical
# placements. What is proven here is that the panel REPORTS the channel's
# numbers rather than deriving its own — no rounding, no re-composition — plus
# the authored passthrough, the copy-not-alias rule, and the three ways the
# placements can be absent (channel down, board uncompilable, no panel), each
# with a stated reason and never an empty list. Whether the numbers are RIGHT
# is the worker's oracle: worker/tests/test_assembly_placements_surface.py
# compares them to the CPL writer's own rows.
test_assembly_placements_surface.gd assertions=27
# The panel's placement memo across a whole-board LOAD, on the real PCBPanel
# with a stub _MinervaIPC node answering from the board it is sent: a load
# leaves board_revision alone, so the memo's second key (the panel's
# data_changed count) is what stops board B being answered with board A's
# placements; a load that lands during an in-flight round trip is not memoized
# either.
test_assembly_placements_memo.gd assertions=19
# A `pins` entry is an OVERRIDE of the like-numbered land and nothing else —
# to_board_dict drops a pin that restates the resolve (an empty name/roles being
# no content, the same emptiness test the worker applies), keeps every deviation
# (moved pad, typed override, symbolic name), writes a coordinate only where the
# pin leaves its land, and leaves a pads-authored or unresolved part alone.
# adopt_resolved re-derives the pin map from the lands, so a part written with no
# pins reopens with all of them and an override-only pin lands on its pad rather
# than on the component origin.
test_pin_override_only.gd assertions=20
# The promote reply's `design_delta` — which AUTHORED facts differ from the file
# being overwritten (per-component changed fields with old and new, components
# and nets added/removed, per-net trace and via counts), against the count-only
# census delta that read all zeros while a component's value flipped. Section 1
# is the pure comparison; Section 2 drives the whole promote over a fake worker
# that is a JSON codec, so the prior file on disk IS the prior design.
test_promote_delta.gd assertions=47
