# Manifest of the gd test suites pcb/scripts/run-gd-tests.sh MUST find under
# pcb/tests/gd/. Line format (v2, bug 019ff2b1fccb + 019fa83e8310):
#
#     <filename> [assertions=N] [real-worker]
#
# - <filename>: exactly as it appears on disk. A suite going missing
#   (deleted, renamed, moved off the glob) or appearing unregistered is a
#   loud, named pre-flight failure in both directions.
# - assertions=N: the suite must report exactly N total assertions
#   (passed+failed). This is what makes `check(true); quit(0)` planted above
#   the real assertions FAIL instead of passing the old `n_pass > 0` floor.
#   When a suite legitimately grows or shrinks, re-pin here in a deliberate,
#   reviewed own-commit — the same act as registering the suite.
# - real-worker: the suite's Results line must prove real_worker_used=true.
#   Its canned subprocess-boundary fallback keeps it runnable on a machine
#   without the pcb-plugin binary, but can never satisfy the gate.
#
# This file is deliberately NOT named test_*.gd (it must not match the
# runner's own `test_*.gd` glob) and NOT executable GDScript.
# See pcb/docs/gd-tests.md.
test_bus_tool.gd assertions=64
test_candidate_canvas.gd assertions=278
test_connectivity_baseline_surface.gd assertions=21
test_epoch6_fix_regressions.gd assertions=9
test_geometric_drc_surface.gd assertions=31
# GA-1 grew this suite (stack_shape_error matrix, stack_for_count,
# inner_index_any, the set_board_layers model/handler section) and retired the
# dead default_two_layer checks; the exact count re-pins at GA testex from the
# first trustworthy run, per the OFC precedent below.
test_layer_stack.gd assertions=105
test_manifest_tool_registration.gd assertions=42
test_pad_synthesis.gd assertions=8
test_parity_bridge.gd assertions=79
test_pcb_annotation_host_semantics.gd assertions=185
test_pcb_apply_route_hints.gd assertions=89
test_pcb_backend_lifecycle.gd assertions=52
test_pcb_bus_geometry.gd assertions=83
test_pcb_canvas_input_probe.gd assertions=123
test_pcb_cutout.gd assertions=38
test_pcb_drc_propose.gd assertions=23 real-worker
# Pinned at OFC testex 2026-08-13 — the first run where all three proved
# real_worker_used=true (fixture repairs: resolvable footprints, authored
# design_rules, in-bounds parts, un-connected BR span; see hint
# 019ff9a0b471).
test_pcb_explicit_propose.gd assertions=68 real-worker
test_pcb_hint_refine_loop.gd assertions=64 real-worker
test_pcb_single_trace_tool.gd assertions=98 real-worker
test_pcb_legacy_annotation_migration.gd assertions=53
test_pcb_panel_layout.gd assertions=147
test_pcb_panel_model.gd assertions=186
test_pcb_panel_tools.gd assertions=206
test_pcb_panel_tools_migration.gd assertions=68
test_pcb_panel_ui.gd assertions=154
test_pcb_pin_inspector.gd assertions=49
# OFC-6 (019ff942f1f8) flipped the two stale pre-cutover .minpcb checks to
# the current core contract — verified 43/0 at OFC testex on the dev
# scaffold.
test_pcb_plugin_smoke.gd assertions=43
test_pcb_route_hint_mcp_parity.gd assertions=19
test_pcb_trace_tool.gd assertions=31
# Re-pinned 28 → 34 with OFC-5's menu-revealed drawing-width section (BT-OFC5,
# 6 checks; the section-registry count check updates in place).
test_pcb_width_prefs.gd assertions=34
test_pcb_workflow_kinds.gd assertions=131
test_production_seam.gd assertions=96
test_proposal_attribution.gd assertions=33
test_routing_workspace_model.gd assertions=168
test_routing_workspace_v2.gd assertions=395
test_trace_identity_delete.gd assertions=213
test_workspace_check.gd assertions=30
test_workspace_ingest.gd assertions=69
test_workspace_persistence.gd assertions=76
test_workspace_tools.gd assertions=1135
test_zone_select_delete.gd assertions=50
test_ux3_contracts.gd assertions=44
test_staged_entities.gd assertions=72
# Re-pinned 34 → 49 with OFC-2's placement-composition section (15 checks),
# then 49 → 68 with OFC-3's provenance + commit-gate section (19 checks),
# then 68 → 82 with OFC-4's board_check census section (14 checks),
# then 82 → 91 with the Codex-1188 repair round (F1 race seams ×4 in 1c,
# F3/F4/F5 census checks ×5 in 1d).
test_draft_composer.gd assertions=91
test_staged_canvas.gd assertions=86
# Pinned at OFC testex 2026-08-13 (first post-await-repair full run).
test_staged_verbs.gd assertions=73
test_draft_doorways.gd assertions=31
test_staged_placement.gd assertions=114
# NEW at epoch GA-6 (bug 019fa0f8d575): the typed-Dictionary guard matrix for
# both panel files' _dict_or_empty. Assertion count pins at the GA testex
# from the first trustworthy run.
test_dict_guard.gd assertions=30
